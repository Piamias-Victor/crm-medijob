# One Referent on a Ligne de suivi

op-medijob stored a free-text `recruteur` and could co-credit two names at 100 % each (matrix totals ≠ agency CA). CRM already has Referent as a User.

Decision: a Ligne de suivi has **one Referent**, picked from CRM Users like Pharmacy / Candidate — not free text, not several Users. Follow-up attributes the line once. Devis-only CA (no linked line) stays on the Mission Referent. Rejected: co-credit (old tool) and a free-text recruteur.
