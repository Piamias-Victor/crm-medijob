// Prisma stub — generator + datasource only.
// Le schéma métier (models, enums) est livré dans l'issue #52.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
