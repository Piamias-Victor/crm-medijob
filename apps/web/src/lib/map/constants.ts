/** Pins above this count render as Leaflet clusters. */
export const MAP_CLUSTER_THRESHOLD = 50

/** Lower = more sensitive wheel/trackpad zoom (Leaflet default ~60). */
export const MAP_WHEEL_PX_PER_ZOOM_LEVEL = 20

/** Leaflet default 40 — 0 reduces trackpad lag. */
export const MAP_WHEEL_DEBOUNCE_MS = 0

export const MAP_ZOOM_SNAP = 0
export const MAP_ZOOM_DELTA = 0.5
