/** Q11 hypo — relance en retard = pas d'ActivityLog depuis N jours (défaut 7). */
export const OVERDUE_FOLLOWUP_DAYS = 7

/** Urgence KPI : startDate dans les prochaines 48 h. */
export const URGENT_WITHIN_HOURS = 48

/** Prefetch centre d'alertes (compteur + aperçu). */
export const ALERT_PREVIEW_LIMIT = 5

export const MS_PER_HOUR = 60 * 60 * 1000
export const MS_PER_DAY = 24 * MS_PER_HOUR
