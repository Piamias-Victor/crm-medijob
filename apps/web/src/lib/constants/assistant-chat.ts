/** Sliding window of UI chat turns sent to the LLM (session only). */
export const CHAT_HISTORY_WINDOW = 8

/** Viewport chrome: main p-6 + PageHeader + gap. */
export const ASSISTANT_CHAT_VIEWPORT_OFFSET = '11rem'
